enum TurnState {
  beforeRoll,
  rolling,
  choosePower,
  powerDynamite,
  powerMineCart,
  powerShiv,
  powerPickaxe,
  sendMinerOrCollectGold,
  turnFinished,
}

enum Powers { dynamite, minecart, shiv, pickaxe, lunchbox }
